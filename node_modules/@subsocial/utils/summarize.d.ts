declare type SummarizeOpt = {
    limit?: number;
    omission?: string;
};
/** Shorten a plain text up to `limit` chars. Split by separators. */
export declare const summarize: (text: string, opts?: SummarizeOpt) => string;
export declare const summarizeMd: (md: string, opts?: SummarizeOpt) => {
    summary: string;
    isShowMore: boolean;
};
export {};
