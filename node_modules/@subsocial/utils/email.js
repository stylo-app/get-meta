"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isValidEmailProvider = exports.validEmailProviders = exports.formatEmail = exports.parseEmail = void 0;
const string_1 = require("./string");
exports.parseEmail = (email) => email === null || email === void 0 ? void 0 : email.trim().toLowerCase().split('@');
exports.formatEmail = (email) => {
    if (!email)
        return '';
    const [username, domain] = exports.parseEmail(email);
    return `${username.replace(/[\W_]/g, '')}@${domain}`;
};
exports.validEmailProviders = [
    'gmail.com',
    'google.com',
    'yahoo.com',
    'protonmail.com',
    'hotmail.com',
    'outlook.com',
    'msn.com',
    'live.com',
    'aol.com',
    'yandex.com',
    'mail.ru'
];
const validEmailSet = new Set(exports.validEmailProviders);
exports.isValidEmailProvider = (email) => {
    if (!email || string_1.isEmptyStr(email))
        return false;
    const [, domain] = exports.parseEmail(email);
    return validEmailSet.has(domain);
};
