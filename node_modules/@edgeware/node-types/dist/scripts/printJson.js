"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const v41_1 = __importDefault(require("../src/spec/v41"));
const v45_1 = __importDefault(require("../src/spec/v45"));
// parse args
const args = process.argv.slice(2);
const network = args[0] || 'mainnet';
function printTypes(network) {
    // TODO: support explicit versions to get v38 e.g.
    const types = network === 'dev' ? v45_1.default.types : v41_1.default.types;
    console.log(JSON.stringify(types, null, 2));
    process.exit(0);
}
if (network === 'mainnet' || network === 'beresheet' || network === 'dev') {
    printTypes(network);
}
else {
    console.error(`Unknown network: ${network}. Please enter "mainnet", "beresheet", or "dev".`);
    process.exit(1);
}
