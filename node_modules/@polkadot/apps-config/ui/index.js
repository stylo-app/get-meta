// Copyright 2017-2021 @polkadot/apps-config authors & contributors
// SPDX-License-Identifier: Apache-2.0
import { chainColors, nodeColors } from "./colors.js";
import { identityNodes } from "./identityIcons/index.js";
export * from "./logos/index.js";

function sanitize(value) {
  return (value === null || value === void 0 ? void 0 : value.toLowerCase().replace('-', ' ')) || '';
}

export function getSystemIcon(systemName) {
  return identityNodes[systemName.toLowerCase().replace(/-/g, ' ')] || 'substrate';
}
export function getSystemChainColor(systemChain, systemName) {
  // eslint-disable-next-line @typescript-eslint/no-unsafe-return
  return chainColors[sanitize(systemChain)] || nodeColors[sanitize(systemName)];
}