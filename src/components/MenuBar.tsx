import React from 'react';
import { Menu } from 'semantic-ui-react';

const Header = () => (
  <Menu pointing
    secondary>
    <Menu.Item>
          Meta getter
    </Menu.Item>
  </Menu>
);

export default Header;
